\usepackage{booktabs}

% Use fontspec package for custom fonts (xelatex or lualatex)
\usepackage{fontspec}

% Set the main font to Arial Narrow (or any other installed font)
\setmainfont{Arial Narrow}
